name: 'little_dwh_tranformations'
version: '1.0.0'

profile: 'little_dwh_tranformations'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:         
  - "target"
  - "dbt_packages"

models:
  little_dwh_tranformations:

    staging:
      +schema: stage
      +materialized: view    


    vault:
      +schema: vault
      +materialized: incremental 


    mart:
      +schema: mart
      +materialized: table  
